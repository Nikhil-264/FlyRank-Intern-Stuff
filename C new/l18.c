// typecasting
#include <stdio.h>
int main()
{
    int a = 5;
    float b = (float)54 / 5; // if float in bracket not written the output would be just 10 not 10.8
    printf("value is %d and %f", a, b);
    return 0;
}
