#include <stdio.h>
#include <math.h>

int main(int argc, char const *argv[])
{
    double a, b;
    printf("enter numver a");
    scanf("%lf", &a);

    printf("enter b");
    scanf("%lf", &b);

    printf("sum is %lf\n ", sin(a));

    return 0;
}
