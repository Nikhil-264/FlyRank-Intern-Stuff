#include <bits/stdc++.h>
using namespace std;

int main()
{
    // // binary and bits start 1
    // cout << INT_MAX << endl;
    // int a = (1LL << 32) - 1; // Wont get calculated as 1 << 32 itself is 33 bit number
    // // Make if long long by adding LL to 1 
    // // as this is signed numbers 1 bit off 32 is used for sign so we get INT_MAX
    // cout << a << endl;// Will print -1 as it cant store this number bcoz its a signed integer so half number s are negative

    // int b = (1LL << 31) - 1;// THis is value of INT_MAX
    // cout << b << endl;
    // unsigned int c = (1LL << 32) - 1;
    // cout << c << endl;
    // return 0;

    // binsry and bits 2
    // long long a = 10011010101;
    // a[0] == is called as MBS = Most Significant Bit
    // a[n - 1] == is called as LBS = Least Significant Bit // Where n is numberf of digits in the number
    // set bits = 1's;
    // unset bits = 0's;

}