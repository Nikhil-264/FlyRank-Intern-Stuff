import pandas as pd
import flask
import pygame

# C:\Users\HP\Desktop\myenv\Scripts\Activate.ps1
# commmand pip freeze will show all the pacakages that are installed in the curent environment
#  the command pip freeze > filename will print all then content in the pip freeze means the packages installed in the filename for exapmle i did pip freeze > requirements.txt
# We can distribute thos file to other users and they can recreate the same environment using: pip install -r requirements.txt it will install all the packages from requirements.txt